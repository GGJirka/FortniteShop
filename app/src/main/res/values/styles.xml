<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="ToolbarLight" parent="android:Theme.Holo">
        <item name="android:homeAsUpIndicator">@drawable/back_icon</item>
    </style>

    <style name="MenuBackground" parent="AppTheme.NoActionBar">
        <item name="android:itemBackground">@color/colorPrimary</item>
        <item name="android:itemTextAppearance">@style/MenuStyle</item>
        <item name="android:divider">@color/colorPrimary</item>
    </style>

    <style name="MenuStyle" parent="style/TextAppearance.AppCompat.Widget.ActionBar.Title">
        <item name="android:textColor">@color/transparentwhite</item>
        <item name="android:textSize">20sp</item>
        <item name="android:divider">@color/colorPrimary</item>

    </style>


</resources>
