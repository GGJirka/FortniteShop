<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <group android:id="@+id/group"
        android:checkableBehavior="single">
        <item
            android:id="@+id/sort_outfit"
            android:icon="@drawable/set_type"
            android:showAsAction="ifRoom"
            android:title="outfits"
           />
        <item
            android:id="@+id/sort_pickaxe"
            android:showAsAction="ifRoom"
            android:title="pickaxes"
           />
        <item
            android:id="@+id/sort_glinder"
            android:showAsAction="ifRoom"
            android:title="gliders"
            />
        <item
            android:id="@+id/sort_emote"
            android:showAsAction="ifRoom"
            android:title="emotes"
            />
    </group>
</menu>